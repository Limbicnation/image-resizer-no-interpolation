# image-resizer-no-interpolation
A Python script for batch resizing images without interpolation, using the Pillow library.

pip install Pillow

## Execution

```python batch_resize_images_without_interpolation.py```
